# 约束模块（平仄句式/押韵/对仗检查）
pypinyin>=0.55
jieba>=0.42

# 语料结构化
python-docx>=1.1

# 测试
pytest>=8.0

# Baseline tokenizer / causal LM
transformers>=4.44,<5
jinja2>=3.1

# 参数高效微调 / 训练调度
# PyTorch 本身与 CPU/CUDA 平台相关，请按 docs/training_setup.md 单独安装。
peft>=0.17,<1
accelerate>=1.0,<2
